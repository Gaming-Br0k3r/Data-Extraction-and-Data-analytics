the Output.xlsx file has already the data for the asked query


in this project i have created a main function named main.py

which you need to run in the vs code or any ide along with all the same files/folders that you have provided earli>

the code runs fluently and creates a folter 'TextFiles' in which all the text from website scraped using bs4 is st>

for each url and after which the code itself performs the data anylysis as defined functions in the program

through which it stores the datasets (like positive score, negative score, etc.) and appends them to the given exc>




P.S. Make sure to install the required packages to run the code.

which are (pip pandas, requests, beautifulsoup4, nltk, openpyxl)
also be aware to use the nltk with punkt already in the commnd